<div class="list-group">
    {{#unless transfers.length}}
        <div class="alert alert-warning">You don't have any transfers yet.</div>
    {{/unless}}
    {{#if transfers.length}}
    <table class="table table-striped table-bordered table-hover">
        <thead>
        <tr>
            <th>Recipient</th>
            <th>Amount</th>
            <th>description</th>
        </tr>
        </thead>
        <tbody>

        {{#each transfers}}
            <tr class="b-transferList__item" data-id="{{id}}">
            <td>
                {{name}}
            </td>
            <td>
                {{amount}}
            </td>
            <td>
                {{description}}
            </td>
            </tr>
        {{/each}}
        </tbody>
    </table>
    {{/if}}
</div>